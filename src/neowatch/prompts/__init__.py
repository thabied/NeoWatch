"""Prompts package.

Central home for versioned system prompts. Keeping prompts in one place (not
scattered as inline strings) makes them reviewable, diffable, and versionable —
the spec calls this out as "system prompt versioning".
"""
