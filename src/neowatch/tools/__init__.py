"""Tools package.

Holds the Claude tool-use schemas and the callable functions they map to. This
is the bridge between "the LLM decided to call a tool" and "Python actually runs
the corresponding data-fetch function".
"""
