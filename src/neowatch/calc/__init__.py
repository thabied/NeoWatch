"""Calc package.

Pure, deterministic orbital-mechanics and risk maths (numpy/scipy). No LLM, no
I/O — same inputs always give the same outputs, which makes it fully unit-testable
and trustworthy as the numerical source of truth for the report.
"""
